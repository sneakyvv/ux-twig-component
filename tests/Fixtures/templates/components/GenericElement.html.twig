<{{ element }}{{ attributes }}>
    {%- block content -%}
        The Generic Element could have some default content, although it does not make sense in this example.
    {%- endblock -%}
</{{ element }}>
